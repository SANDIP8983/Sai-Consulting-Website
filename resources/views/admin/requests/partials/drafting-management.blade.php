@if($customerRequest->processing?->uses_drafting_workflow) @php($p=$customerRequest->processing)<div class="card border-0 shadow-sm mb-4"><div class="card-header bg-white fw-semibold">ડ્રાફ્ટિંગ · Drafting</div><div class="card-body"><form method="POST" action="{{ route('admin.requests.processing.drafting.update',$customerRequest) }}">@csrf @method('PATCH')<div class="row g-3">@foreach(['draft_started_at'=>'Draft Started Date','draft_ready_at'=>'Draft Ready Date','customer_verification_at'=>'Customer Verification Date','final_draft_at'=>'Final Draft Date'] as $field=>$label)<div class="col-sm-6"><label class="form-label">{{ $label }}</label><input type="date" name="{{ $field }}" value="{{ old($field,$p->{$field}?->toDateString()) }}" class="form-control"></div>@endforeach<div class="col-12"><label class="form-label">Correction Note</label><textarea name="correction_note" rows="2" class="form-control">{{ old('correction_note',$p->correction_note) }}</textarea><div class="form-text">Internal only.</div></div><div class="col-12"><label class="form-label">Internal Drafting Note</label><textarea name="drafting_internal_note" rows="2" class="form-control">{{ old('drafting_internal_note',$p->drafting_internal_note) }}</textarea><div class="form-text">Never visible to customers.</div></div><div class="col-12"><label class="form-label">Customer-visible Drafting Remark</label><textarea name="drafting_customer_remark" rows="2" class="form-control">{{ old('drafting_customer_remark',$p->drafting_customer_remark) }}</textarea></div></div><button class="btn btn-outline-primary mt-3">Update Drafting Information</button></form></div></div>@endif
