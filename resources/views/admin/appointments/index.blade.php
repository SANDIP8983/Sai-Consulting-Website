@extends('layouts.admin')
@section('title','Appointments')
@section('content')
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="d-flex justify-content-between align-items-center mb-3"><h1 class="h3 mb-0">Appointments</h1><a class="btn btn-primary" href="{{ route('admin.appointments.create') }}">New Appointment</a></div>
<div class="card mb-3"><div class="card-body"><form class="row g-2"><div class="col-lg-4"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Reference, customer or mobile"></div><div class="col-lg-2"><select class="form-select" name="status"><option value="">All statuses</option>@foreach($statuses as $s)<option value="{{ $s->value }}" @selected(request('status')===$s->value)>{{ $s->label() }}</option>@endforeach</select></div><div class="col-lg-2"><select class="form-select" name="service_id"><option value="">All services</option>@foreach($services as $s)<option value="{{ $s->id }}" @selected(request('service_id')==$s->id)>{{ $s->name_en }}</option>@endforeach</select></div><div class="col-lg-2"><input type="date" class="form-control" name="from" value="{{ request('from') }}"></div><div class="col-lg-2"><button class="btn btn-outline-primary w-100">Filter</button></div></form></div></div>
<div class="card"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Reference</th><th>Date / Time</th><th>Customer</th><th>Service</th><th>Status</th><th>Source</th></tr></thead><tbody>@forelse($appointments as $a)<tr><td><a href="{{ route('admin.appointments.show',$a) }}">{{ $a->reference_no }}</a></td><td>{{ $a->scheduled_at->format('d M Y, g:i A') }}</td><td>{{ $a->customer_name }}<br><small>{{ $a->mobile }}</small></td><td>{{ $a->service->name_en }}</td><td><span class="badge text-bg-secondary">{{ $a->status->label() }}</span></td><td>{{ ucfirst($a->source) }}</td></tr>@empty<tr><td colspan="6" class="text-center py-4">No appointments found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $appointments->links() }}</div>
@endsection
