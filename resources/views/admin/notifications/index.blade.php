@extends('layouts.admin')
@section('title','Notification Log')
@section('content')
<h1 class="h2">Customer Notification Log</h1><form class="card card-body border-0 shadow-sm mb-4" method="GET"><div class="row g-2"><div class="col-md-3"><input name="q" value="{{ request('q') }}" class="form-control" placeholder="Reference Number"></div><div class="col-md-2"><select name="milestone" class="form-select"><option value="">All milestones</option>@foreach($milestones as $m)<option value="{{ $m->value }}" @selected(request('milestone')===$m->value)>{{ $m->label() }}</option>@endforeach</select></div><div class="col-md-2"><select name="channel" class="form-select"><option value="">All channels</option><option value="email">Email</option><option value="whatsapp">WhatsApp</option></select></div><div class="col-md-2"><select name="status" class="form-select"><option value="">All statuses</option>@foreach(['pending','sent','failed','skipped'] as $s)<option value="{{ $s }}">{{ str($s)->title() }}</option>@endforeach</select></div><div class="col-md-1"><input type="date" name="date_from" value="{{ request('date_from') }}" class="form-control"></div><div class="col-md-1"><input type="date" name="date_to" value="{{ request('date_to') }}" class="form-control"></div><div class="col-md-1"><button class="btn btn-primary">Filter</button></div></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Reference</th><th>Milestone</th><th>Channel</th><th>Recipient</th><th>Status</th><th>Time</th><th>Failure</th></tr></thead><tbody>@forelse($deliveries as $d)<tr><td>{{ $d->event->customerRequest->reference_no }}</td><td>{{ $d->event->milestone->label() }}</td><td>{{ str($d->channel)->title() }}</td><td>{{ $d->recipient_masked ?: '—' }}</td><td>{{ str($d->status)->title() }}</td><td>{{ ($d->sent_at ?: $d->created_at)->format('d M Y, g:i A') }}</td><td>{{ $d->failure_category ? str($d->failure_category)->replace('_',' ')->title() : '—' }}</td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-4">No notification deliveries found.</td></tr>@endforelse</tbody></table></div>@if($deliveries->hasPages())<div class="card-body">{{ $deliveries->links() }}</div>@endif</div>
@endsection
